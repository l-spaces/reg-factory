# ============================================================
# reg-factory 环境变量模板
# 用法：cp .env.example .env  然后填入你自己的 key。
# .env 已被 .gitignore 忽略，不会进版本库。
# 真实的进程环境变量优先于 .env（.env 只在变量未设置时生效）。
# ============================================================

# ---------------- Clash Verge 控制面（节点切换/代理出口）----------------
# 在 Clash Verge -> 设置 -> External Controller 开启 API，并设置 secret。
CLASH_API=http://127.0.0.1:9097
CLASH_SECRET=
CLASH_PROXY=http://127.0.0.1:7897
# global 模式下真正决定出口的组名（默认 GLOBAL）
CLASH_GROUP=GLOBAL

# ---------------- BitBrowser 本地 API ----------------
BITBROWSER_API=http://127.0.0.1:54345

# ---------------- 短信接码平台 ----------------
# 主用：firefox.fun
SMS_TOKEN=
# 备用：hero-sms.com
HERO_SMS_API_KEY=

# ---------------- 打码平台（可选，按需启用）----------------
CAPSOLVER_API_KEY=
EZCAPTCHA_API_KEY=

# ---------------- Outlook 邮箱来源 ----------------
# 闪客云邮箱卡密（用接口批量取号时需要）
OUTLOOK_CARD=
# Outlook 自注册用的住宅代理池（user:pass@host:port），多个用换行或逗号分隔，可留空
OUTLOOK_PROXIES=

# ---------------- 备用域名邮箱（一般用不到，可留空）----------------
MAIL_DOMAIN=
MAIL_API_BASE=
MAIL_ADMIN_USER=admin
MAIL_ADMIN_PASS=
MAIL_AUTH_TOKEN=
MAIL_NEW_PASS=
